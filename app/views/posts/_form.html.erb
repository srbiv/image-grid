<%= simple_form_for @post, :html => { :multipart => true } do |f| %>
  <%= f.input :image %>
  <%= f.input :title %>
  <%= f.input :description %>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
